# Android multi display — example

Host app demonstrating **`android_multi_display`** (`com.azemdev.multidisplay` on Android).

See the parent [`../README.md`](../README.md) for integration steps.

```bash
flutter pub get
flutter run
```
